{
  "name": "meta.txt",
  "version": "0.7.0",
  "description": "llms.txt, but for your repo — a living markdown knowledge base browsable via npx meta.txt",
  "type": "module",
  "bin": {
    "meta.txt": "bin/meta.ts"
  },
  "files": [
    "bin",
    "src",
    "ui/dist",
    "README.md",
    "CHANGELOG.md",
    "LICENSE"
  ],
  "scripts": {
    "dev": "bun scripts/dev.ts",
    "dev:ui": "cd ui && bun run dev",
    "dev:server": "bun run --watch bin/meta.ts",
    "build:ui": "cd ui && bun run build",
    "gen:assets": "bun scripts/gen-assets.ts",
    "build:bin": "bun build --compile --target=bun --outfile=dist/meta bin/meta.ts",
    "build": "bun run build:ui && bun run gen:assets && bun run build:bin",
    "start": "bun run bin/meta.ts",
    "prepublishOnly": "bun run build:ui && bun run gen:assets",
    "release": "bun run build:ui && bun run gen:assets && npm publish"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/andreyantipov/meta.txt.git"
  },
  "keywords": [
    "markdown",
    "docs",
    "viewer",
    "cli"
  ],
  "author": "Andrey Antipov",
  "license": "MIT",
  "engines": {
    "bun": ">=1.1.0"
  },
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5"
  },
  "dependencies": {
    "@agentclientprotocol/sdk": "^0.19.2",
    "gray-matter": "^4.0.3",
    "node-html-parser": "^7.1.0"
  }
}
